.container
  .row
    .col-md-12
      %h1= DeviceType.model_name.human(count: 2)

  .row
    .col-md-12
      %ul
        - @device_types.each do |device_type|
          %li= link_to h(device_type.name), url_for(device_type)
