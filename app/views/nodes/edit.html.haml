.container
  %h1= t :edit_h, thing: Node.model_name.human

  = form_tag url_for(@node), method: :put, multipart: true do
    = render partial: 'form', locals: { node: @node }

    = submit_tag t(:update), class: 'btn btn-primary'
    = link_to t(:cancel), url_for(@node), class: 'btn btn-warning'
