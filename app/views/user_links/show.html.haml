.container
  .row
    .col-md-12
      %h1= h @user_link.name

  .row
    .col-md-7
      = render partial: 'shared/edit_delete', locals: { record: @user_link }

      %p= link_to @user_link.url, @user_link.url

    .col-md-5
      %p.small
        - if @user_link.user.name
          = link_to @user_link.user.name, url_for(@user_link.user)
