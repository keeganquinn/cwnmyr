- content_for :meta_title, 'Reset Password'

%h2 Reset Password

%p Forgot your password? Enter your email address here to reset it.

= turbo_form resource, url: user_password_path do |f|

  = f.input :email, required: false
  = f.submit 'Reset Password'

= render 'users/shared/links'
