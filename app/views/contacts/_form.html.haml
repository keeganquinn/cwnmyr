%p Hidden contacts will not be available to the public.

= f.input :name
= f.input :hidden

.row
  .col-md-6
    = f.input :email
  .col-md-6
    = f.input :phone

= f.input :notes

- unless current_user.groups.empty?
  = f.association :group

= f.hidden_field :user_id, value: contact&.user_id || current_user.id
= hidden_field_tag :authenticity_token, form_authenticity_token
